/**
 * parity barrel — the seam the management shell and ChatHost import from.
 *
 * Naming rule: everything here is `kds-*` classes / `parity` modules, so
 * integration never collides with hand-written app styles.
 */
export * from "./presentation";
export * from "./surfaces";
